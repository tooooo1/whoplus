import React from 'react';
import { NumberWrapper, NumberFunc } from '../components';

const NumberMain = () => (
        <NumberWrapper>
            <NumberFunc>
            
            </NumberFunc>
        </NumberWrapper>
);

export default NumberMain;